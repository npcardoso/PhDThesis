echo "Edit me!!" && exit 1

PATH=$PATH: #INSERT DIR

INPUT_DIR=
OUTPUT_DIR=

MIN_COMP=
MAX_COMP=
MIN_TRAN=
MAX_TRAN=

ITERATIONS=

PARAMS=

EXEC=

SIZES=`sizes.sh $MIN_COMP $MAX_COMP $MIN_TRAN $MAX_TRAN`
if [[! -d $INPUT_DIR ]]; then
  INPUTS=`inputs.sh $INPUT_DIR "$SIZES" $ITERATIONS "$PARAMS"`
else
  INPUTS=`find $INPUT_DIR`
fi
run.sh $OUTPUT_DIR $EXEC "$INPUTS"
